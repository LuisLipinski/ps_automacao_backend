# URLs locais (não contém segredos)
PS_EMPRESA_URL=http://localhost:8081
PS_CONTRATO_URL=http://localhost:8082
PS_USER_URL=http://localhost:8083
PS_LOGIN_URL=http://localhost:8084
PS_ORCHESTRATOR_URL=http://localhost:8085
PS_GATEWAY_URL=http://localhost:8080

# Defina localmente ou via GitHub/CI secrets. Nunca versionar valores reais.
PS_EMPRESA_INTERNAL_KEY=replace-me
PS_CONTRATO_INTERNAL_KEY=replace-me
PS_USER_INTERNAL_KEY=replace-me

# Conta técnica de QA usada apenas nos testes autenticados cross-service.
QA_LOGIN_EMAIL=replace-me
QA_LOGIN_PASSWORD=replace-me

# Fixture de QA para a matriz destrutiva de autorização do PS_User.
# Deve apontar para o primeiro MASTER ativo de uma empresa exclusiva de QA.
QA_PRIMARY_MASTER_USER_ID=replace-me

# Habilite apenas em ambiente efêmero/QA: cria e remove dados de teste.
RUN_DESTRUCTIVE_INTEGRATION=false
